;; if the machine does not have internet access
;; it means that MELPA packages cannot be uses
;; so comment the second statement
(org-babel-load-file "~/.emacs.d/cmn.org")
(org-babel-load-file "~/.emacs.d/config.org")

